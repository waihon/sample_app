<% provide(:title, "All users") %>
<h1>All users</h1>

<%= will_paginate @users %>

<ul class="users">
  <%# User.all.each do |user %>
    <%#= render "user", user: user %>
  <%# end %>
  <%= render @users %>
</ul>

<%= will_paginate @users %>
